version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mysql-ebank
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: walid123
      MYSQL_DATABASE: E-BANK
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - spring-network

volumes:
  mysql_data:

networks:
  spring-network:
    driver: bridge
